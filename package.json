{
	"name": "noglinks",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"crypto": "^1.0.1",
		"hono": "^4.7.2",
		"jsonwebtoken": "^9.0.2"
	},
	"devDependencies": {
		"wrangler": "^3.109.1"
	}
}
